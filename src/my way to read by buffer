public void loadCorpus(String corpusFilename) throws IOException, FileNotFoundException {
		
		BufferedReader br = new BufferedReader ( new FileReader (corpusFilename));
		String st;
		while ((st=br.readLine()) !=null ){
			cor.insert(st);
			
			br.close();
			
			}
		

		}
